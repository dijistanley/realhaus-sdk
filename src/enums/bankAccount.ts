export enum Vendors {
  PAYPAL = "PAYPAL",
  STRIPE = "STRIPE",
}

export enum BankAccountPurpose {
  PAYMENTS = "PAYMENTS",
  PAYOUTS = "PAYOUTS",
}

export enum AccountConnectionState {
  INCOMPLETE = "INCOMPLETE",
  COMPLETE = "COMPLETE",
}
